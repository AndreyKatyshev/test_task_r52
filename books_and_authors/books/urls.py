from django.urls import path

from . import views

app_name = 'books'

urlpatterns = [
    # path('download', views.download, name='download'),
    # path('authors/<int:post_id>/', views.all_books_by_authors, name='authors'),
]